"use client";

import Link from "next/link";
import { useRouter, useSearchParams } from "next/navigation";
import { useState } from "react";
import { ResponsiveContainer, AreaChart, Area, CartesianGrid, XAxis, YAxis, Tooltip } from "recharts";
import type { ColumnDef } from "@tanstack/react-table";
import { Download, RefreshCw } from "lucide-react";
import { adminPost, dateTime, downloadAdminCsv, money, type AdminRun, type Page } from "@/lib/admin";
import { AdminHeading, AdminState, ConfirmAction, DataTable, Status, useAdminData } from "@/components/admin-ui";
import { WorkflowGraph } from "@/components/admin-graph";

type Overview = { run_counts: Record<string, number>; local_usage: { cost_microusd: number; tokens: number; calls: number }; hourly: { bucket_start: string; total_cost_microusd: number; total_tokens: number }[]; aggregates_stale: boolean; aggregates_refreshed_at: string | null; openrouter_credits: { status: string; remaining_microusd: number | null; checked_at?: string } };
export function AdminOverview() {
  const { data, loading, error, reload } = useAdminData<Overview>("/overview");
  const chart = data?.hourly.map(row => ({ time: new Date(row.bucket_start).toLocaleTimeString([], { hour: "2-digit" }), cost: row.total_cost_microusd / 1_000_000 })) || [];
  return <><AdminHeading eyebrow="OPERATIONS / 24 HOURS" title="System overview" description="Local run activity and ledger cost, with OpenRouter credit shown separately." actions={<button className="admin-secondary" onClick={() => void reload()}><RefreshCw size={15} /> Refresh</button>} /><AdminState loading={loading} error={error}><div className="admin-grid"><div className="admin-card"><div className="admin-card-label">Runs started</div><div className="admin-card-value">{Object.values(data?.run_counts || {}).reduce((sum, value) => sum + value, 0)}</div><p>{Object.entries(data?.run_counts || {}).map(([key, value]) => `${key}: ${value}`).join(" · ") || "No runs yet"}</p></div><div className="admin-card"><div className="admin-card-label">Local cost</div><div className="admin-card-value">{money(data?.local_usage.cost_microusd)}</div><p>From the usage ledger · 24h</p></div><div className="admin-card"><div className="admin-card-label">Tokens</div><div className="admin-card-value">{(data?.local_usage.tokens || 0).toLocaleString()}</div><p>{data?.local_usage.calls || 0} model calls</p></div><div className="admin-card"><div className="admin-card-label">OpenRouter credit</div><div className="admin-card-value">{money(data?.openrouter_credits.remaining_microusd)}</div><p>{data?.openrouter_credits.status || "unavailable"}{data?.openrouter_credits.checked_at ? ` · ${dateTime(data.openrouter_credits.checked_at)}` : ""}</p></div></div>{data?.aggregates_stale && <div className="admin-banner admin-banner-warning" role="status">Analytics aggregates are stale or unavailable. Local totals above read directly from the ledger.</div>}<div className="admin-two-col"><section className="admin-panel"><div className="admin-panel-head"><h2>Hourly local cost</h2><span className="admin-muted">Last reconciliation {dateTime(data?.aggregates_refreshed_at)}</span></div>{chart.length ? <div style={{ width: "100%", height: 250 }} role="img" aria-label="Hourly local cost chart; data also available in Analytics"><ResponsiveContainer><AreaChart data={chart}><CartesianGrid stroke="#2a3342" strokeDasharray="3 3" /><XAxis dataKey="time" stroke="#aab4c3" fontSize={11} /><YAxis stroke="#aab4c3" fontSize={11} /><Tooltip contentStyle={{ background: "#151a23", border: "1px solid #3a4659" }} /><Area dataKey="cost" stroke="#8b7cf6" fill="#8b7cf633" /></AreaChart></ResponsiveContainer></div> : <p className="admin-muted">No usage has been aggregated yet.</p>}</section><section className="admin-panel"><h2>Go to</h2><ul className="admin-list"><li><Link href="/admin/runs">Inspect run traces →</Link></li><li><Link href="/admin/agents">Review agent drafts →</Link></li><li><Link href="/admin/models">Browse model routes →</Link></li><li><Link href="/admin/settings">Manage budgets and kill switch →</Link></li></ul></section></div></AdminState></>;
}

const runColumns: ColumnDef<AdminRun, unknown>[] = [
  { accessorKey: "product", header: "Product", cell: context => <Link href={`/admin/runs/${context.row.original.id}`}>{context.row.original.product}</Link> },
  { accessorKey: "status", header: "Status", cell: context => <Status value={context.row.original.status} /> },
  { accessorKey: "initiator_type", header: "Initiator" },
  { accessorKey: "created_at", header: "Created", cell: context => dateTime(context.row.original.created_at) },
  { accessorKey: "total_tokens", header: "Tokens", cell: context => context.row.original.total_tokens.toLocaleString() },
  { accessorKey: "total_cost_microusd", header: "Local cost", cell: context => money(context.row.original.total_cost_microusd) },
];

export function AdminRuns() {
  const search = useSearchParams(); const router = useRouter(); const [cursor, setCursor] = useState<string | null>(null);
  const status = search.get("status") || ""; const initiator = search.get("initiator_type") || ""; const q = search.get("q") || "";
  const [query, setQuery] = useState(q);
  const params = new URLSearchParams({ limit: "25" }); if (status) params.set("status", status); if (initiator) params.set("initiator_type", initiator); if (q) params.set("q", q); if (cursor) params.set("cursor", cursor);
  const { data, loading, error, reload } = useAdminData<Page<AdminRun>>(`/runs?${params}`);
  function update(name: string, value: string) { const next = new URLSearchParams(search.toString()); if (value) next.set(name, value); else next.delete(name); setCursor(null); router.replace(`/admin/runs?${next}`); }
  return <><AdminHeading eyebrow="OPERATIONS / RUNS" title="Run explorer" description="Inspect each task, attempt, tool call, and ledger entry without exposing stored prompts." actions={<button className="admin-secondary" onClick={() => void reload()}><RefreshCw size={15} /> Refresh</button>} /><section className="admin-panel"><form className="admin-filter" onSubmit={event => { event.preventDefault(); update("q", query); }}><label>Status<select value={status} onChange={event => update("status", event.target.value)}><option value="">All</option>{["queued", "running", "complete", "partial", "failed", "cancelled"].map(item => <option key={item}>{item}</option>)}</select></label><label>Initiator<select value={initiator} onChange={event => update("initiator_type", event.target.value)}><option value="">All</option>{["public", "admin", "admin_evaluation", "system_fixture"].map(item => <option key={item}>{item}</option>)}</select></label><label>Product<input value={query} onChange={event => setQuery(event.target.value)} placeholder="Search product" /></label><button className="admin-secondary">Apply</button></form><AdminState loading={loading} error={error} empty={data?.items.length === 0}><DataTable data={data?.items || []} columns={runColumns} caption="Admin runs" /><div className="admin-pager"><button disabled={!cursor} onClick={() => setCursor(null)}>First page</button><button disabled={!data?.next_cursor} onClick={() => setCursor(data?.next_cursor || null)}>Next page</button></div></AdminState></section></>;
}

type RunDetail = AdminRun & { snapshot: { id: string; workflow_version_id: string; budget_policy_version_id: string; agents: unknown[]; model_policies: unknown[] } | null; tasks: { id: string; task_key: string; status: string; agent_version_id: string | null; current_attempt: number }[]; dependencies: { from: string; to: string }[]; attempts: { id: string; task_run_id: string; number: number; status: string; error_code: string | null; duration_ms: number | null; prompt_hash: string | null }[]; usage: { id: string; task_attempt_id: string; actual_model: string | null; actual_provider: string | null; total_tokens: number | null; total_cost_microusd: number | null; usage_status: string }[]; tool_invocations: { id: string; tool_key: string; status: string; duration_ms: number | null }[]; workspace_id: string | null; report_id: string | null; public_report_active: boolean };
export function AdminRunDetail({ id }: { id: string }) {
  const { data, loading, error, reload } = useAdminData<RunDetail>(`/runs/${id}`);
  const [message] = useState("");
  return <><AdminHeading eyebrow="OPERATIONS / RUN TRACE" title={data?.product || "Run trace"} description={`Run ${id}`} actions={<Link className="admin-secondary" href="/admin/runs">← Runs</Link>} /><AdminState loading={loading} error={error} empty={!data}>{data && <><div className="admin-grid"><div className="admin-card"><div className="admin-card-label">Status</div><Status value={data.status} /><p>Created {dateTime(data.created_at)}</p></div><div className="admin-card"><div className="admin-card-label">Tasks</div><div className="admin-card-value">{data.tasks.length}</div><p>{data.attempts.length} attempts</p></div><div className="admin-card"><div className="admin-card-label">Local cost</div><div className="admin-card-value">{money(data.total_cost_microusd)}</div><p>{data.pending_usage_count} pending ledger entries</p></div><div className="admin-card"><div className="admin-card-label">Tokens</div><div className="admin-card-value">{data.total_tokens.toLocaleString()}</div><p>{data.model_call_count} calls</p></div></div><div className="admin-panel"><div className="admin-panel-head"><h2>Recovery controls</h2><div className="admin-actions">{["queued", "running", "cancelling"].includes(data.status) && <ConfirmAction label="Cancel run" title="Cancel this run" description="Cancel the active run and stop dispatching remaining tasks." confirmation="cancel run" danger onConfirm={async () => { await adminPost(`/runs/${id}/cancel`, {}); await reload(); }} />}{data.report_id && data.public_report_active && <ConfirmAction label="Revoke report" title="Revoke public report" description="The public report token will stop resolving." confirmation="revoke report" danger onConfirm={async () => { await adminPost(`/reports/${data.report_id}/revoke`, {}); await reload(); }} />}</div></div>{message && <p role="status">{message}</p>}</div><div className="admin-panel"><h2>Task DAG</h2><WorkflowGraph dag={{ tasks: data.tasks.map(task => ({ task_key: task.id, handler: task.task_key, dependencies: data.dependencies.filter(dep => dep.to === task.id).map(dep => dep.from) })) }} /><div className="admin-trace" style={{ marginTop: 18 }}>{data.tasks.map(task => <article className="admin-trace-item" key={task.id}><strong>{task.task_key} <Status value={task.status} /></strong><small>{task.id} · attempt {task.current_attempt}</small><details><summary>Attempts and recovery</summary>{data.attempts.filter(attempt => attempt.task_run_id === task.id).map(attempt => <p key={attempt.id} className="admin-muted">#{attempt.number} · {attempt.status} · {attempt.error_code || "no error"} · {attempt.duration_ms ?? "—"} ms · prompt hash {attempt.prompt_hash || "—"}</p>)}{["failed", "timed_out"].includes(task.status) && <ConfirmAction label="Retry task" title="Retry failed task" description="The runtime will create another bounded attempt." confirmation="retry task" onConfirm={async () => { await adminPost(`/tasks/${task.id}/retry`, {}); await reload(); }} />}</details></article>)}</div></div><div className="admin-two-col"><div className="admin-panel"><h2>Model usage</h2><ul className="admin-list">{data.usage.map(item => <li key={item.id}><div><strong>{item.actual_model || "Pending model"}</strong><br /><span className="admin-muted">{item.actual_provider || "unknown provider"} · {item.usage_status} · {item.total_tokens ?? "—"} tokens</span></div><span>{money(item.total_cost_microusd)}</span></li>)}</ul>{data.usage.length === 0 && <p className="admin-muted">No model calls recorded.</p>}</div><div className="admin-panel"><h2>Tool audits and snapshot</h2><ul className="admin-list">{data.tool_invocations.map(item => <li key={item.id}><span>{item.tool_key} <Status value={item.status} /></span><span className="admin-muted">{item.duration_ms ?? "—"} ms</span></li>)}</ul><p className="admin-muted">Workflow {data.snapshot?.workflow_version_id || "—"}</p><p className="admin-muted">Budget {data.snapshot?.budget_policy_version_id || "—"}</p>{data.workspace_id && <Link href={`/admin/knowledge?workspace=${data.workspace_id}`}>Open workspace graph →</Link>}</div></div></>}</AdminState></>;
}

type AnalyticsRow = { bucket_start: string; dimension_key: string; request_count: number; error_count: number; total_tokens: number; total_cost_microusd: number };
export function AdminAnalytics() {
  const search = useSearchParams(); const router = useRouter();
  const granularity = search.get("granularity") || "day"; const dimension = search.get("dimension") || "all";
  const dimensionKey = search.get("dimension_key") || ""; const start = search.get("start") || ""; const end = search.get("end") || "";
  const params = new URLSearchParams({ granularity, dimension, limit: "100" });
  if (dimensionKey) params.set("dimension_key", dimensionKey); if (start) params.set("start", start); if (end) params.set("end", end);
  const exportParams = new URLSearchParams(params); exportParams.delete("limit");
  const { data, loading, error } = useAdminData<Page<AnalyticsRow>>(`/analytics?${params}`);
  const [message, setMessage] = useState("");
  function update(name: string, value: string) { const next = new URLSearchParams(search.toString()); next.set(name, value); router.replace(`/admin/analytics?${next}`); }
  function updateDate(name: string, value: string) { const next = new URLSearchParams(search.toString()); if (value) next.set(name, `${value}T00:00:00Z`); else next.delete(name); router.replace(`/admin/analytics?${next}`); }
  const chart = [...(data?.items || [])].reverse().map(item => ({ name: new Date(item.bucket_start).toLocaleDateString(), cost: item.total_cost_microusd / 1_000_000 }));
  const columns: ColumnDef<AnalyticsRow, unknown>[] = [
    { accessorKey: "bucket_start", header: "Bucket", cell: context => dateTime(context.row.original.bucket_start) },
    { accessorKey: "dimension_key", header: "Key" }, { accessorKey: "request_count", header: "Calls" },
    { accessorKey: "error_count", header: "Errors" }, { accessorKey: "total_tokens", header: "Tokens" },
    { accessorKey: "total_cost_microusd", header: "Local cost", cell: context => money(context.row.original.total_cost_microusd) },
  ];
  return <><AdminHeading eyebrow="INTELLIGENCE / ANALYTICS" title="Usage analytics" description="Hourly and daily projections reconciled from the local usage ledger." actions={<button className="admin-secondary" onClick={() => { downloadAdminCsv(`/analytics/export.csv?${exportParams}`, "reviewlens-analytics.csv").catch(value => setMessage(value.message)); }}><Download size={16} /> Export CSV</button>} /><section className="admin-panel"><div className="admin-filter"><label>Interval<select value={granularity} onChange={event => update("granularity", event.target.value)}><option value="day">Daily</option><option value="hour">Hourly</option></select></label><label>Group by<select value={dimension} onChange={event => update("dimension", event.target.value)}>{["all", "model", "provider", "agent", "operation", "initiator"].map(item => <option key={item}>{item}</option>)}</select></label><label>Exact key<input value={dimensionKey} onChange={event => update("dimension_key", event.target.value)} placeholder="Optional dimension key" /></label><label>From (UTC)<input type="date" value={start.slice(0, 10)} onChange={event => updateDate("start", event.target.value)} /></label><label>To (UTC)<input type="date" value={end.slice(0, 10)} onChange={event => updateDate("end", event.target.value)} /></label></div>{message && <div role="alert" className="admin-banner admin-banner-error">{message}</div>}<AdminState loading={loading} error={error} empty={data?.items.length === 0}>{chart.length > 0 && <div style={{ width: "100%", height: 240 }} role="img" aria-label="Local cost over time; exact figures follow in the table"><ResponsiveContainer><AreaChart data={chart}><CartesianGrid stroke="#2a3342" strokeDasharray="3 3" /><XAxis dataKey="name" stroke="#aab4c3" fontSize={11} /><YAxis stroke="#aab4c3" fontSize={11} /><Tooltip contentStyle={{ background: "#151a23", border: "1px solid #3a4659" }} /><Area dataKey="cost" stroke="#35d0ba" fill="#35d0ba22" /></AreaChart></ResponsiveContainer></div>}<DataTable data={data?.items || []} columns={columns} caption="Usage analytics" /></AdminState></section></>;
}
